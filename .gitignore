# SIWA monorepo — Next.js at root, Laravel in backend/
node_modules
.next
out
next-env.d.ts
*.tsbuildinfo
.env*
!.env.example
.DS_Store
*.log

# Data KK asli warga (PII — arsip sumber, JANGAN pernah di-commit)
/kk
# public/kk TIDAK di-ignore: salinan foto KK ter-seed ikut repo private.
# Aksesnya dikunci middleware signature (KK_LINK_SECRET) — bukan URL publik bebas.
